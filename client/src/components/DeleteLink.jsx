import axios from "axios";
import React from "react";

const DeleteLink = () => {
  const deleteLink = async () => {
    try {
      // Write logic here later
    } catch (error) {
      console.log(error);
    }
  };
  return <div>DeleteLink</div>;
};

export default DeleteLink;
